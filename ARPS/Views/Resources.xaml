<UserControl x:Class="ARPS.Views.Resources"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ARPS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Side - Foder Tree -->
        <!--<TreeView x:Name="treeView_Directorys" Grid.Column="0" BorderThickness="0" Margin="0 0 7 0" Padding="0 10" SelectedItemChanged="TreeViewSelectedItemChange"/>-->
        <TreeView x:Name="treeView_Directorys" Grid.Column="0" BorderThickness="0" Margin="0 0 7 0" Padding="0 10"/>

        <!-- Splitter-->
        <GridSplitter Grid.Column="0" BorderBrush="{StaticResource ArgesGrauBlau_hell}" BorderThickness="4"></GridSplitter>
        
        <!-- Right Side -->
        <DockPanel Grid.Column="1" Background="White">
            <!-- 1. Row - Folder and Path -->
            <Grid DockPanel.Dock="Top" Margin="0 7">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Height="25" Source="{StaticResource Img_Folder}"/>
                <StackPanel Grid.Column="1">
                    <Label x:Name="lbl_FolderName" Content="Apollon" Padding="0" HorizontalAlignment="Left" VerticalAlignment="Top" 
                           FontSize="18" FontWeight="Bold" Foreground="{StaticResource ArgesBlau}"/>
                    <Label x:Name="lbl_FolderPath" Content="\\apollon" Padding="0" HorizontalAlignment="Left" VerticalAlignment="Bottom" Foreground="{StaticResource ArgesBlau}"/>
                </StackPanel>
            </Grid>

            <Border DockPanel.Dock="Top" BorderThickness="2" BorderBrush="{StaticResource ArgesGelb}" />

            <!-- 2. Row - Owner -->
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="{StaticResource String_DE_Owner}" HorizontalAlignment="Left" VerticalAlignment="Top" 
                       FontWeight="Bold" Foreground="{StaticResource ArgesBlau}"/>
                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" x:Name="img_OwnerIcon" Height="16" Width="16"/>
                    <Label Grid.Column="1" x:Name="lbl_OwnerName" Content="Kein Besitzer gefunden" HorizontalAlignment="Left"/>
                </Grid>
            </Grid>
            <!-- 3. Row - NTFS Access rights -->
            <!-- TODO -->

            <!-- 4. Row - Accounts with permissions -->
            <Label DockPanel.Dock="Top" Content="{StaticResource String_DE_AccountsWithPermissions}" FontWeight="Bold" Foreground="{StaticResource ArgesBlau}" FontSize="18"/>
            <Border DockPanel.Dock="Top" BorderThickness="2" BorderBrush="{StaticResource ArgesGelb}"></Border>
            
            <!-- Account with permissions -->
            <!--<ListView DockPanel.Dock="Bottom" x:Name="listView_AccountWithPermissions" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn>
                            <GridViewColumn.CellTemplate>
                                
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>-->

        </DockPanel>
        
        
    </Grid>
</UserControl>
